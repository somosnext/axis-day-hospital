"use client";import{useState}from"react";import{Button}from"@/components/ui/button";
export function StorageUploader(){const[state,setState]=useState("");async function upload(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;setState("Enviando…");const fd=new FormData();fd.set("file",file);const r=await fetch("/api/admin/upload",{method:"POST",body:fd});if(!r.ok){setState("Arquivo recusado ou falha no envio.");return}setState("Imagem enviada. Atualize a página para vê-la na galeria.");location.reload()}return <div className="mb-6 flex flex-wrap items-center gap-4 bg-white p-5"><label className="text-sm font-semibold text-navy">Nova imagem <input className="ml-3 text-sm" type="file" accept="image/jpeg,image/png,image/webp" onChange={upload}/></label>{state&&<span className="text-xs text-ink/55">{state}</span>}<Button variant="outline" className="ml-auto text-navy" href="#gallery-list">Gerenciar abaixo</Button></div>}
