import{AdminHeader}from"@/components/admin/page-header";import{LeadsManager}from"@/components/admin/leads-manager";import{createSupabaseServerClient}from"@/lib/supabase/server";export default async function Page(){const s=await createSupabaseServerClient();const{data}=await s.from("leads").select("*").order("created_at",{ascending:false}).limit(500);return <><AdminHeader eyebrow="CRM" title="Leads" description="Busca, filtros, status, notas e exportação dos contatos recebidos."/><LeadsManager initial={data||[]}/></>}
