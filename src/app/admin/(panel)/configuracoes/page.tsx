import{AdminHeader}from"@/components/admin/page-header";import{SettingsForm}from"@/components/admin/settings-form";import{confirmedSettings}from"@/config/site";import{createSupabaseServerClient}from"@/lib/supabase/server";
export default async function Page(){const s=await createSupabaseServerClient(),{data}=await s.from("site_settings").select("*").limit(1).maybeSingle();return <><AdminHeader eyebrow="Configurações" title="Dados institucionais e SEO" description="Campos vazios não são publicados como fatos. Telefone, WhatsApp e redes só aparecem quando cadastrados."/><SettingsForm initial={data||confirmedSettings}/></>}
